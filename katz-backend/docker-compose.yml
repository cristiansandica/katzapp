version: '3.8'
services:
  postgres:
    image: postgres
    container_name: my-postgres
    restart: always
    environment:
      POSTGRES_USER: katz_user
      POSTGRES_PASSWORD: katz_pass
      POSTGRES_DB: katz_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: